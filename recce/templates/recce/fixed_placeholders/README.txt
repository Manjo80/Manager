Placeholders for fixed view images (used when no image uploaded yet).

Expected filenames (PNG recommended):
- front.png
- rear.png
- top.png
- side.png

Put your images into this folder and run:
  python manage.py collectstatic

Or just upload images per install option in the UI (preferred).
